# live-server-kit hosted-integration

Mounts a `LiveServerClient` into a host-style service object and mirrors a route summary.

Run:

```bash
node kits/live-server-kit/examples/hosted-integration/index.js
```
