# cloudflare-tunnel-kit live-or-edge

Checks whether `cloudflared` is available and prints the local-only fallback if it is not.

Run:

```bash
node kits/cloudflare-tunnel-kit/examples/live-or-edge/index.js
```
