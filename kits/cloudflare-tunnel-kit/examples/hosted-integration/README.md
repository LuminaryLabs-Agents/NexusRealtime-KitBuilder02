# cloudflare-tunnel-kit hosted-integration

Writes the same runtime manifest shape that NexusRealtime can consume.

Run:

```bash
node kits/cloudflare-tunnel-kit/examples/hosted-integration/index.js
```
