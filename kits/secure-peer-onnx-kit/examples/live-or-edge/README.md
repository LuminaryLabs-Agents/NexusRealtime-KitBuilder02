# secure-peer-onnx-kit live-or-edge

Demonstrates degraded startup in `up-to` mode when one selected model fails and another still warms.

Run:

```bash
node kits/secure-peer-onnx-kit/examples/live-or-edge/index.js
```
